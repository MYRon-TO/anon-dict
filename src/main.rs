use anon_dict::db::DataBase;

#[tokio::main]
async fn main() {
    // let db = DataBase::new().await.expect("Failed to connect db");
    // let rows = db
    //     .fuzzy_search_from_word("appl", 10)
    //     .await
    //     .expect("Failed to fetch row");
    // for row in rows {
    //     println!("{:?}", row);
    // }

}
